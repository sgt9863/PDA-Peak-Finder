# 開発ロードマップ

## 現在地(v0.1)— アーキテクチャと解析パイプライン

ARW フォーマット非依存の骨格と全解析段を実装済み。合成データで全機能が動作します。

- [x] 共通データモデル(`models.py`)と例外階層
- [x] リーダーインターフェース+レジストリ(`reader/base.py`)、ARW スタブ
- [x] ピーク検出・特性計算(RT / FWHM / 面積)
- [x] UV スペクトル抽出・λmax
- [x] 分析間ピークトラッキング
- [x] CSV 出力
- [x] 可視化(クロマトグラム / コンター / スペクトル / トラッキング)
- [x] パイプライン統合・CLI(`analyze` / `demo`)
- [x] 合成データによる全段テスト

## v0.2 — ARW リーダー実装(サンプル提供後)

**トリガー:** 実際の ARW サンプルファイルの入手。

- [ ] ARW ヘッダ/数値ブロックの構造を確認(区切り文字・波長軸の向き・単位)
- [ ] `reader/arw.py` の `read()` を実装(`reader/arw.py` の docstring のチェックリストに従う)
- [ ] メタデータ抽出(`injection_id`・sample name・channel・取得日時)
- [ ] 単位変換(必要なら mAU→AU / 秒→分)と軸の単調増加保証
- [ ] `tests/fixtures/` に実 ARW フィクスチャとゴールデン値テストを追加
- [ ] `sniff()` を内容ベースに強化(ファイル名だけに依存しない判定)

この段階で `pda-peaks analyze data/*.arw` が実データで動作します。

## v0.3 — 解析精度と堅牢性

- [ ] ベースライン補正(ALS など)とドリフト除去
- [ ] ショルダー/重なりピークの分離(デコンボリューション)
- [ ] ノイズ推定に基づく適応的な検出しきい値
- [ ] スペクトル純度(ピーク均一性)指標の算出
- [ ] トラッキングの改良(λmax・スペクトル類似度・面積の併用、参照インジェクション整列)

## v0.4 — 運用性

- [ ] 設定ファイル(YAML/TOML)によるパラメータ管理
- [ ] バッチ処理・並列読み込み
- [ ] HTML/PDF レポート出力
- [ ] 他社/汎用フォーマット(CSV エクスポート等)のリーダー追加

## v1.0 以降(将来)

- [ ] GUI(ワークフローが固まった後)
- [ ] ライブラリ照合による化合物同定
- [ ] 定量(検量線)対応

## 設計上の指針

- ファイル形式依存は `reader` に閉じ込め、他モジュールは `PDAData` のみに依存する。
- 新機能は既存モジュールの責務内に収め、モジュール間は `models.py` の型で連携する。
- 各段は合成データ(`testing.synthetic_pdadata`)で単体検証できる状態を保つ。
